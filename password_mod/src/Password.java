import java.util.Scanner;

class Password{
    public static void main(String[] args){

        // String inputPassword;
        // String newPassword;

        // Scanner scnr = new Scanner(System.in);

        // System.out.println("Please enter a password ");
    
        // inputPassword = scnr.next();
        
        // for(int i = 0; i < inputPassword.length(); i++){
        //     char currChar = inputPassword.charAt((i));
        //     currChar = (String)currChar;
        //     System.out.println(currChar);
        //     // System.out.println(currChar.getClass().getName());
        //     // inputPassword.replace(currChar, "a");
        // }
        // System.out.println(inputPassword.getClass().getName());
        // scnr.close();
        int x = 18;
        while (x > 0) {
           System.out.print(x);
           x = x / 3; 
        }
}
}
